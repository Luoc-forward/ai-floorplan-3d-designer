# AI 户型 3D 设计器 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 文档名称 | AI 户型 3D 设计器（AI FloorPlan 3D Designer） |
| 文档版本 | V0.9（初稿，待评审） |
| 编写日期 | 2026-09-19 |
| 文档状态 | 草稿 Draft |
| 适用平台 | Web（PC / 平板浏览器优先，兼顾手机） |

---

## 1. 产品概述

### 1.1 项目背景

用户在看房、买房、租房或准备装修时，拿到的通常只是一张 2D 户型图（售楼处彩页、中介截图、CAD 导出图、手绘草图）。平面户型图对普通用户并不直观，难以感知：

- 真实空间尺度、开间进深、采光与动线；
- 家具摆下后是否拥挤、柜门能否打开；
- 不同墙地面配色、软装风格的实际效果。

传统做法是请设计师用 CAD / 3ds Max / SketchUp 手工建模，周期数小时到数天，成本高、迭代慢。本产品希望用「**上传一张户型图 → AI 自动识别 → 秒级生成可漫游 3D 户型 → 用户自由编辑家具与配色**」的方式，把这一过程压缩到分钟级，且零专业门槛。

### 1.2 产品一句话定位

> 拍一张 / 传一张户型图，AI 自动识墙、识门窗、识尺寸，立刻生成可以走进去的 three.js 3D 家；任何人都能拖拽家具、换色换材质、漫游浏览并分享。

### 1.3 核心价值

| 角色 | 价值 |
|---|---|
| 业主 / 租客 | 提前"入住"，直观判断户型好坏、尺寸是否够用 |
| 房产中介 | 零成本把平面图变 3D 带看，提升线上看房转化率 |
| 家装 / 家居行业 | 客户自助试摆家具、试配色，沉淀线索与商品曝光 |
| 室内设计师 | 快速出草模与客户对齐需求，减少前期沟通成本 |

### 1.4 名词解释

| 名词 | 含义 |
|---|---|
| 识别底图 | 用户上传的原始户型图（位图 / PDF 转图） |
| 墙体矢量化 | 把图像中的墙线识别为带坐标、厚度、高度的线段数据 |
| 校准（Calibration） | 用户指定一段已知实际长度（如"这面墙 3600mm"），解算像素↔毫米比例 |
| 开洞（Opening） | 墙体上门洞、窗洞的布尔切除 |
| Gizmo | 3D 编辑中用于移动 / 旋转 / 缩放的轴向操纵器 |
| 漫游模式 | 第一人称在户型内行走浏览的模式 |
| 方案（Scheme） | 同一户型下的一套家具与配色布置，可存多套 |

---

## 2. 目标用户与典型场景

### 2.1 目标用户

1. **C 端 · 购房/租房者（核心）**：非专业，手机/平板操作，要求"傻瓜式"。
2. **C 端 · 准备装修的业主**：关注家具布局、收纳、配色、预算。
3. **B 端 · 房产中介 / 新房销售**：高频、批量出图，需要分享链接与二维码。
4. **B 端 · 家装公司 / 家居电商导购**：需要真实商品模型、品牌物料、留资。
5. **B 端 · 室内设计师**：把它当快速草模工具，后续可能需要导出 CAD/BIM。

### 2.2 核心使用场景

- **场景 A：线上看房**——中介群里发了张户型截图，小张上传后 10 秒走进房子，发现玄关太窄、次卧放不下 1.5m 床。
- **场景 B：装修前期**——李女士上传自家户型，分别做"现代简约"和"奶油风"两套方案，发给家人投票。
- **场景 C：买家具前**——王先生在卖场看中沙发，用商品库里的真实尺寸模型放进客厅，确认走道还剩 65cm 才下单。
- **场景 D：中介批量制作**——经纪人一天上传 20 个房源，自动生成 720° 链接挂到房源详情页。
- **场景 E：现场拍照**——用户只有纸质户型图，手机拍照（有透视畸变），系统自动矫正后识别。

---

## 3. 产品目标与衡量指标

### 3.1 本期目标（MVP~V1.0）

- 规则户型图（黑白线稿 / 彩色置业图）**上传后 15 秒内**生成可接受人工微调的 3D 户型；
- 用户无需培训即可完成：校准 → 漫游 → 摆 3 件家具 → 换一个墙面配色 → 分享；
- 核心链路转化率：上传 → 成功生成 3D ≥ **85%**；生成 → 发生一次编辑 ≥ 60%。

### 3.2 关键质量指标（AI 识别，针对规则户型测试集）

| 指标 | 目标值 |
|---|---|
| 墙体节点/连接 识别 F1 | ≥ 95% |
| 门（含开向/类型）识别准确率 | ≥ 90% |
| 窗识别准确率 | ≥ 90% |
| 尺寸数字 OCR 准确率 | ≥ 95% |
| 房间功能（客/卧/厨/卫）推断准确率 | ≥ 85% |
| 识别结果"零修改直接可用"占比 | ≥ 60%，微调（≤5 处）后可用 ≥ 90% |

### 3.3 性能指标

| 指标 | 目标 |
|---|---|
| 编辑器首屏可交互（含 3D 引擎） | ≤ 3s（4G 网络，首加载后二次进入 ≤ 1.5s） |
| 识别耗时（常见 1~3 居，P95） | ≤ 15s，支持进度反馈 |
| 3D 场景帧率 | PC 端 60fps；移动端 ≥ 30fps |
| 单方案存档大小 | ≤ 5MB（不含贴图缓存） |

---

## 4. 用户主流程（Happy Path）

```text
上传户型图
   │
   ├─ 拍照 / 相册 / 文件（JPG/PNG/PDF）/ 选示例户型
   │
   ▼
图像预处理（去噪、透视矫正、转正、增强墙线）
   │
   ▼
AI 识别（墙体→门窗→尺寸标注→房间语义）  ── 进度条 + 分步状态
   │
   ▼
2D 识别结果校验页（半透明原图叠加识别矢量）
   │  ├─ 拖拽修正墙/门/窗（置信度低处以黄色高亮）
   │  └─ 尺寸校准：拖一条已知尺寸线输入毫米数（或 OCR 自动填充）
   ▼
一键生成 3D（拉伸墙体、开门窗洞、铺地、默认灯光）
   │
   ▼
3D 编辑器（默认鸟瞰 45° 进场动画 → 落到入户门人视高度）
   │
   ├─ 漫游：WASD / 触屏摇杆，碰撞检测，小地图传送
   ├─ 家具：左侧素材库 → 拖入房间 → Gizmo 移动/旋转/缩放/复制
   ├─ 配色：点选墙面/地板/家具 → 色板 / 材质 / AI 配色方案
   ├─ 光照：时间滑块控制日照，一键夜景开灯
   └─ 标注：测距、面积、自动尺寸链
   │
   ▼
保存方案 / 多方案对比 / 截图 / 720° 分享链接 / 二维码
```

兜底分支：识别置信度过低或用户选择"我自己画"→ 进入**半自动描墙模式**（沿底图点画墙体、点一下放门放窗、输入总长），仍可生成 3D。

---

## 5. 功能需求

> 优先级：**P0** = MVP 必做；**P1** = V1.0；**P2** = 后续版本。

### 5.1 户型图上传与预处理【P0】

| 编号 | 需求 | 说明 |
|---|---|---|
| F1-1 | 多来源上传 | 本地文件（JPG/JPEG/PNG/WEBP，单张 ≤ 20MB）、PDF（首页/全部页转栅格）、手机拍照、拖拽到窗口、粘贴板（Ctrl+V）截图 |
| F1-2 | 拍照透视矫正 | 对手机拍摄的纸质图自动检测纸张四角，做透视变换转正；用户可拖动四角微调 |
| F1-3 | 图像增强 | 灰度化、去阴影、自适应二值化、墙线加粗修复、去噪（去除折痕/网纹） |
| F1-4 | 方向与比例 | 自动判断横/竖向；提示用户确认图中北向（影响日后日照模拟，P1） |
| F1-5 | 多图拼接 | 支持分幅拍的超大户型图按特征点拼接为一张（P2） |
| F1-6 | 示例户型 | 提供若干示例图供未准备图片的用户立即体验 |
| F1-7 | 手绘草图 | 支持线条较规整的手绘户型识别；识别不可用时引导至描墙模式（P1） |

### 5.2 AI 结构识别【P0 为核心能力】

**识别对象清单：**

| 类别 | 识别内容 |
|---|---|
| 墙体 | 外墙、内墙、墙厚（图上双线墙取双线间距；单线墙给默认值）、承重墙标记（黑色填充墙，P1） |
| 门 | 平开门（含开启弧线与开向）、推拉门、折叠门、子母门、入户门、阳台门联窗 |
| 窗 | 普通窗、飘窗（凸窗）、落地窗、转角窗；窗台高/窗高按规范默认值 |
| 洞口 | 开放式厨房垭口、无门门洞 |
| 房间 | 闭合空间分割 + 功能语义（客厅、餐厅、主卧、次卧、厨房、卫生间、阳台、玄关、书房、储物间）；依据图中文字标签与固定设施推断 |
| 尺寸标注 | 尺寸数字（OCR）、尺寸界线/引出线位置；开间、进深、墙体总长、门窗定位尺寸 |
| 固定设施 | 厨房橱柜/灶台/水槽、马桶、淋浴、洗手台、浴缸、楼梯（复式/LOFT）、管井、烟囱 |
| 其他 | 标高、楼层、指北针、面积文字（如"主卧 12.5㎡"）、梁柱（P1） |

**识别处理流水线（技术侧实现路径，产品上对用户表现为分步进度）：**

1. **预处理**：矫正、二值化、倾斜校正（deskew）；
2. **几何检测**：线段检测 + 墙线语义分割（深度学习模型），合并共线线段，提取双线墙中线与厚度；
3. **图结构优化**：线段求交、T/L/X 型节点闭合、直角化（Manhattan 约束，主流户型均为正交结构）、小缺口修补；
4. **门窗检测**：在墙线上检测缺口与门弧符号，分类门型与开向；检测三线/四线窗符号；
5. **OCR**：识别尺寸数字与房间文字、面积、标高；
6. **尺寸解算**：把 OCR 数字与尺寸界线配对，建立像素↔毫米比例尺，反推各段墙长；无标注时以入户门宽 900mm 等常规尺寸做估计并提示用户校准；
7. **房间划分与语义**：闭合环搜索成房间，结合文字标签与厨卫设施符号推断功能；
8. **置信度评估**：逐元素（墙/门/窗/尺寸）输出 confidence，低于阈值（如 0.75）在校验页黄色高亮、列入"待确认 N 项"清单。

| 编号 | 需求 | 优先级 |
|---|---|---|
| F2-1 | 全量识别上述墙体/门窗/房间/尺寸 | P0 |
| F2-2 | 输出每元素置信度并高亮低置信项 | P0 |
| F2-3 | 识别中分步进度反馈（解析图像→识别墙体→识别门窗→读取尺寸→生成空间） | P0 |
| F2-4 | 厨卫固定设施识别并直接生成占位模型 | P1 |
| F2-5 | 复式/LOFT 楼梯、多层识别 | P2 |
| F2-6 | 承重墙识别（影响后期"砸墙"功能与提示） | P1 |
| F2-7 | 识别结果复用：手动修正结果回流，作为后续模型优化样本（需用户授权） | P1 |

### 5.3 2D 识别校验与编辑【P0】

用户生成 3D 前的最后一道关卡，原则：**校对比重画快**。

- **叠加显示**：底图半透明居中，识别出的矢量墙/门/窗叠加，可切换"只看识别结果 / 双图对比 / 侧拉窗放大"。
- **墙体编辑**：点选墙体拖动端点、调整墙厚、合并/拆分/删除；沿底图点击两点新增墙体；支持正交吸附、端点吸附、平行/垂直约束。
- **门编辑**：点击门洞改类型（平开/推拉/折叠）、翻转开向与左右开、调宽度；门在墙上拖动自动改洞位置。
- **窗编辑**：拖动窗位置/宽度、调窗台高与窗高、转飘窗。
- **尺寸校准**：
  - 自动模式：OCR 读到的尺寸自动解算比例尺，顶部提示"已读取 12 个尺寸，比例尺 1px≈3.6mm"；
  - 手动模式：用户在任一已知长度墙体上拖出校准线，输入毫米数，全屋等比缩放；
  - 冲突提示：两处尺寸推得的比例尺偏差 >3% 时提示选择以哪个为准。
- **房间命名**：点击房间改名称/功能，联动默认家具与材质（卫生间防滑砖、厨房瓷砖）。
- **全局操作**：撤销/重做（≥ 50 步）、缩放平移、快捷键、删除、完成后进入 3D；随时可退回本页且保留 3D 编辑（重新生成时提示"将清空已摆家具，可另存为新方案"）。
- **问题清单面板**：按"未闭合墙段 / 重叠墙 / 尺寸缺失 / 疑似门识别成窗"列出，点击定位，提供一键修复建议。【P1】

### 5.4 3D 户型生成【P0】

| 编号 | 需求 | 说明 |
|---|---|---|
| F4-1 | 参数化墙体 | 按墙线、墙厚、层高（默认 2800mm，可改）拉伸几何体；门洞窗洞真实开洞（布尔），非贴图假洞 |
| F4-2 | 门窗模型 | 平开门板+门框按开向生成（默认 40mm 厚）；窗含窗框+半透明玻璃；阳台栏杆（默认 1100mm） |
| F4-3 | 地面/天花 | 每个房间独立地面多边形（便于分别铺材质）；默认平顶天花可一键隐藏，方便鸟瞰 |
| F4-4 | 默认参数 | 见附录 A：层高、门高 2100、窗台 900、窗高 1400、外墙 200/内墙 100 等，可全局设置 |
| F4-5 | 初始镜头 | 生成后播放 1.5s 鸟瞰下降动画，落位入户门内侧、人视高度 1.6m |
| F4-6 | 单位体系 | 内部统一毫米，坐标原点为户型外接矩形左下角，Y 轴向上 |
| F4-7 | 厨卫硬装 | 按识别结果生成橱柜、灶台、水槽、马桶、台盆等低模占位，可替换可删除（P1） |
| F4-8 | 梁/柱/吊顶/踢脚线 | 按图或手动添加（P1/P2） |

### 5.5 3D 漫游【P0】

- **操作方式**
  - PC：第一人称 WASD 移动、鼠标控制视角（Pointer Lock）、Shift 加速、空格/阶梯辅助；按住中键环绕；
  - 移动设备：左下虚拟摇杆移动、右侧拖拽转视角、双指缩放（调整 FOV）；
  - 提供"鸟瞰 / 透视环绕 / 第一人称漫游"三模式切换按钮。
- **碰撞与移动**：
  - 人物胶囊（半径 250mm）与墙体/门窗/家具做碰撞，贴墙滑动；
  - 门可 E 键 / 点击开合（带阻尼动画），推拉门平移；
  - 台阶/楼梯自动跨步；复式楼层可上下（P2）；
  - 限制不能穿墙、不能走出外墙；阳台窗处阻断。
- **导航辅助**：2.5D 小地图（角落，可收放）显示当前位置与朝向，点击房间名/缩略图**点地传送**（免长距离走路）；已浏览房间打勾。
- **漫游体验**：
  - 行走时有轻微头部起伏与视野抖动（可关）；
  - 人视高度可调（1.50~1.75m），并有"坐姿 1.2m / 站立"切换，方便感受沙发/床的视线；
  - 漫游路径录制：自动沿"入户→客厅→主卧→厨卫"导览，用于生成分享视频（P1）。

### 5.6 家具与软装系统【P0 基础，P1 丰富】

**素材库（左侧面板）**

- 分类：客厅（沙发、茶几、电视柜…）、餐厅、卧室、书房、厨房、卫浴、灯具、窗帘、家电、绿植装饰、定制柜体、门窗构件；
- 搜索、品牌筛选、风格筛选（现代/奶油/原木/轻奢/北欧/中式）、尺寸筛选；
- 官方库约 **300+ 常用模型（P0 先 80+）**，glTF/GLB + Draco 压缩，贴图分档加载；
- 收藏夹、最近使用、本项目已用列表。

**摆放与变换**

| 编号 | 功能 | 优先级 |
|---|---|---|
| F6-1 | 从库拖拽到 2D 平面图或 3D 视窗落点放置 | P0 |
| F6-2 | Gizmo 移动/旋转/缩放，吸附地面，旋转默认 15° 步进（按 Shift 自由转） | P0 |
| F6-3 | 墙面吸附：柜体/挂画/空调自动贴墙并避开门洞，可切换贴哪面墙 | P0 |
| F6-4 | 复制、多选（框选/Shift）、组合/解组、镜像翻转、删除、置顶置底 | P0 |
| F6-5 | 撤销/重做、快捷键（W/E/R 切换移/旋/缩，Ctrl+D 复制，Del 删除） | P0 |
| F6-6 | 碰撞提示：家具重叠时红色描边警告（允许强行放置，仅提示）；开门/抽屉展开范围预览 | P1 |
| F6-7 | 对齐与分布：多选后等距分布、边缘对齐 | P1 |
| F6-8 | 尺寸驱动：属性面板直接输入长/宽/高/离地高度数值 | P1 |
| F6-9 | 家具显隐 / 按房间隔离编辑 / 锁定防误触 | P1 |
| F6-10 | 自定义模型上传（GLB/OBJ，限 30MB），自动缩略图 | P2 |

**家具属性与编辑**

- 几何尺寸（W×D×H）、离地高度、旋转角、所在房间；
- 材质/颜色替换（见 5.7）；
- 可动部件：柜门、抽屉可点击开合（模型需带节点约定，P1）；
- 商品信息：品牌、型号、真实尺寸、价格、链接（B 端商城对接，P2）；
- 备注文字（如"要留插座"）。

**AI 辅助布置（P1/P2）**

- P1：**一键智能布局**——选定房间与风格，根据房间尺寸和通行规范自动摆入成套家具（床+床头柜+衣柜+书桌），用户可"换一批"；
- P1：选中某件家具点"相似替换"；
- P2：AI 软装整套生成（文生布置：输入"日式原木风、要婴儿床"）；
- P2：收纳/动线体检（"床头柜距门仅 30cm，建议调整"）。

### 5.7 配色与材质系统【P0】

- **可涂装对象**：内墙、外墙（外立面，P1）、各房间地面、天花、门、窗框、柜体与家具多材质槽、踢脚线。
- **取色入口**：选中对象 → 属性面板"颜色/材质"；
  - 纯色：色轮 + HEX/RGB 输入 + 最近使用色；
  - 材质库 PBR：乳胶漆（多色）、木纹、瓷砖、大理石、地毯、布艺、皮革、金属、玻璃；支持贴图缩放（砖缝密度）与粗糙度调节；
  - 支持**滴管吸色**，在场景中吸取已有配色；
- **智能配色**：
  - 预设主题：现代简约、奶油原木、莫兰迪、轻奢、工业风、新中式等 8~12 套，一键应用到全屋或当前房间；
  - AI 配色（P1）：基于主色自动生成"墙浅地中家私深"等协调方案，锁定某件家具反推全屋配色；
- **涂刷交互**：支持"刷墙模式"，点击哪个面上哪个色；Shift+点击同房间全部墙面；
- 材质选择实时渲染，不破坏光照；移动端降级为简化材质保持帧率。

### 5.8 光照与环境【P1】

- 日照模拟：时间轴 0~24h 拖动，太阳方位/高度/色温联动窗光斑；按城市经纬度+日期计算真实太阳轨迹；
- 室内灯：默认吸顶灯（点击开/关），素材库可加吊灯/台灯/筒灯带，灯具发光对场景有真实照明；
- 一键"日景 / 黄昏 / 夜景"；夜景自动开灯并显示窗外暗色天空；
- HDRI 环境贴图选择、环境光强度、阴影开关（移动端默认关软阴影）；
- 后期：环境光遮蔽（AO）、轻微泛光，开关可控。

### 5.9 标注与测量【P0/P1】

| 功能 | 优先级 |
|---|---|
| 两点测距（磁吸端点/墙边），实时显示毫米，可连续测 | P0 |
| 房间净面积、周长自动显示，选中房间即看 | P0 |
| 自动尺寸链：恢复/重排户型周边与房间开间进深标注 | P1 |
| 3D 文字标注（用户备注，如"这里放冰箱位 700"） | P1 |
| 净高标注、门洞高度标注 | P1 |

### 5.10 视角、出图与分享【P0/P1】

- **视角预设**：鸟瞰、45° 透视、每个房间一键机位、人视漫游；
- **截图**：当前画面导出 PNG（可选 2K/4K、可选水印与 Logo）；P0
- **平面图导出**：带尺寸标注的 PNG/PDF（P1），DXF/DWG（P2）；
- **720° 全景**：每个机位渲染全景图，生成可在手机上陀螺仪浏览的分享链接（P1）；
- **导览视频**：沿录制动线导出 MP4（P2）；
- **分享**：链接 + 二维码，权限（公开/密码/指定手机），被分享者可直接漫游并"复制为我的方案"再编辑；P1
- **多方案管理**：同一户型下 N 个方案 Tab（方案A 现代 / 方案B 原木），切换对比（P1 支持双屏左右对比，P2）。

### 5.11 项目与账户【P0/P1】

- 游客可用（本地缓存自动存草稿，刷新不丢）；登录后云端同步；
- 项目列表：封面截图、户型名、更新时间、方案数；复制、删除、重命名；
- 版本历史（P2）：自动保存每 2 分钟一次，可回滚；
- B 端后台：批量房源、企业 Logo/水印、数据看板（P2）。

---

## 6. 信息架构与界面要点

### 6.1 页面结构

```text
首页（项目列表 / 新建：上传区 + 示例户型）
 └─ 识别校验页（2D）
      └─ 3D 编辑工作台（核心页）
           ├─ 顶部栏：方案名 | 撤销重做 | 视角切换 | 保存 | 分享 | 截图
           ├─ 左侧：素材库 / 材质库 / AI 助手 Tab
           ├─ 中央：three.js 视口（底部模式切换：鸟瞰/环绕/漫游）
           ├─ 右侧：属性面板（随选中对象变化：墙/门/窗/家具/房间）
           └─ 底部/角落：小地图、问题清单、2D 校准回退入口
```

### 6.2 关键交互原则

1. **零学习成本**：首次进入用 3 步气泡引导（拖动转视角 → 拖家具 → 点墙换色）；
2. **模式互斥清晰**：漫游模式下隐藏编辑 Gizmo，编辑模式下禁止行走，避免误操作；
3. **危险操作可撤销**：重识别、清空家具等不可逆动作强提示 + 自动另存；
4. **移动端适配**：手机端面板改为底部抽屉，素材库全屏卡片化，保证单手可完成核心链路；
5. **空状态引导**：空房间中央浮现"从左侧拖入家具，或试试一键智能布局"。

---

## 7. 核心数据模型（方案 JSON）

统一坐标系：毫米，原点为户型包围盒左下角，Z 为进深方向，Y 向上。

```json
{
  "version": "1.0",
  "projectId": "p_10086",
  "name": "阳光花园 3-2-1801",
  "unit": "mm",
  "createdAt": "2026-09-19T01:50:00+08:00",
  "settings": { "wallHeight": 2800, "exteriorWallThickness": 200,
                "interiorWallThickness": 100, "eyeHeight": 1600,
                "northAngle": 0, "geoLocation": { "lat": 31.23, "lng": 121.47 } },
  "calibration": { "pxPerMm": 0.278, "source": "ocr" },
  "walls": [
    { "id": "w1", "sourceWall": true, "thickness": 200,
      "from": [0, 0], "to": [7200, 0], "height": 2800,
      "holes": [
        { "id": "h1", "type": "door", "offset": 300, "width": 900,
          "height": 2100, "door": { "kind": "swing", "openSide": "right",
          "openSideInward": true, "openDegrees": 30 } },
        { "id": "h2", "type": "window", "offset": 4200, "width": 1800,
          "sill": 900, "height": 1400 }
      ],
      "confidence": 0.98 }
  ],
  "rooms": [
    { "id": "r1", "name": "客厅", "type": "living_room",
      "polygon": [[0,0],[7200,0],[7200,3600],[0,3600]],
      "areaM2": 25.92,
      "materials": { "wall": "mat_paint_01", "floor": "mat_wood_03", "ceiling": "mat_white" } }
  ],
  "fixtures": [
    { "id": "f1", "type": "sofa", "modelId": "lib_sofa_042",
      "pos": [2400, 0, 1800], "rotationDeg": 90, "scale": [1,1,1],
      "size": [2200, 850, 900], "materialOverrides": { "fabric": "#B7A794" },
      "roomId": "r1" }
  ],
  "lights": [
    { "id": "l1", "type": "sun", "time": "14:00", "intensity": 1.2 },
    { "id": "l2", "type": "ceiling", "pos": [3600, 2700, 1800], "on": true }
  ],
  "sourceImage": { "objectKey": "img/xxx.jpg", "w": 2480, "h": 1754 }
}
```

---

## 8. 技术方案概要

### 8.1 总体架构

```text
┌─────────────── 前端（Web 单页应用）────────────────┐
│ 上传/矫正  │  2D 校验编辑器(SVG/Canvas2D)           │
│ three.js 3D 引擎：场景图/材质/光照/碰撞/Gizmo/漫游  │
│ 状态管理 + 方案 JSON + IndexedDB 草稿               │
└───────────────┬─────────────────────────────────────┘
                │ HTTPS（上传图 / 轮询任务 / 存取方案）
┌───────────────▼─────────────────────────────────────┐
│ 识别服务：预处理 → 墙线分割模型 → 几何优化 →        │
│ 门窗检测 → OCR → 尺寸解算 → 房间语义 → 结果 JSON    │
│ 异步任务队列（GPU worker）                           │
└──────────────────────────────────────────────────────┘
```

### 8.2 前端

- 语言/框架：TypeScript + **three.js**（或 react-three-fiber 封装，按团队栈定）；构建 Vite；
- 关键技术点：
  - 墙体几何：沿墙线路径生成带洞拉伸体（ExtrudeGeometry + 洞口路径，或预生成顶点网格），避免运行时 CSG 布尔的性能损耗；
  - 渲染：InstancedMesh 处理重复构件；模型 glTF + Draco/meshopt，KTX2 压缩贴图；LOD；
  - 碰撞：基于墙体/家具 AABB 与胶囊体的八叉树（three-mesh-bvh）查询；
  - 2D 编辑器：Canvas2D/SVG 独立一套矢量数据，与 3D 共享同一份户型数据模型；
  - 漫游：PointerLockControls 思路自实现 + 触屏虚拟摇杆；
  - 性能：视锥剔除、默认关闭实时软阴影改烘焙/AO 贴图、Web Worker 做几何与识别结果后处理；
- 兼容：Chrome/Edge/Safari 最近两个大版本；WebGL2，WebGL1 不支持时提示；iOS Safari 15+。

### 8.3 识别服务

- 模型组合（任一可行路线，以评测集数据决定）：
  - 墙线/结构：语义分割（U-Net/DeepLab 系）或栅格线段检测（如 LSD/LineTransformer 思路）+ 规则后处理；
  - 门窗符号：目标检测/小模型分类；门弧用符号规则；
  - OCR：PaddleOCR 等通用 OCR + 户型图标注样式微调；
  - 房间语义：图上文字 + 设施检测器 + 规则推断；
- 输出强约束的结构化 JSON（第 7 节），服务端做几何合法性校验（闭合、不交叉、尺寸自洽），不合法自动修复或降级为低置信度交人工；
- 异步任务：POST 上传返回 taskId，轮询/SSE 推送分步进度。

### 8.4 接口草案

```text
POST /api/recognition/tasks          multipart 上传图片 + 参数
  -> { taskId }
GET  /api/recognition/tasks/{id}     -> { stage, progress, status }
GET  /api/recognition/tasks/{id}/result
  -> 方案 JSON（walls/rooms/holes/calibration/confidence...）
POST /api/projects                   保存方案
GET  /api/projects/{id}              读取方案（含多 scheme）
POST /api/render/panorama            提交全景渲染任务（P1）
```

---

## 9. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能预算 | 场景三角面 ≤ 150 万、DrawCall ≤ 300（PC）；内存 ≤ 1.5GB；识别服务单卡并发按容量规划 |
| 可用性 | 核心编辑器前端崩溃后草稿可恢复；自动存草稿间隔 ≤ 30s |
| 兼容 | 1920×1080 起步，支持 4K 屏与触控；手机端保证核心链路（看+漫游+简单拖拽） |
| 无障碍 | 色盲安全（置信度不只靠颜色，附图标）、键盘可达主要操作（P2） |
| 隐私安全 | 户型图可能含住址信息：传输加密；用户可删除原图与识别结果；未授权不用于训练；B 端支持合同保密 |
| 版权 | 家具模型/材质贴图需有商用授权；用户上传模型的侵权风险由用户协议约定 |
| 国际化 | 中/英文，毫米与英尺英寸双单位（P2） |

---

## 10. 埋点与数据指标（摘要）

- 漏斗：访问首页 → 上传 → 识别成功 → 校准通过 → 进入 3D → 首次编辑（家具/配色）→ 保存 → 分享；
- 质量：识别平均耗时、各元素人工修改率、校准触发率、识别失败原因分布；
- 参与：人均家具操作次数、配色方案应用率、漫游时长与房间访问热力、7 日留存；
- B 端：分享链接打开率、全景停留时长。

---

## 11. 验收标准（关键用例节选）

1. 给定 3 张标准测试户型（一居/两居/三居黑白线稿各 1），上传后 P95 ≤ 15s 返回结果，墙体 F1 ≥ 95%，且全部通过服务端几何合法性校验；
2. 低置信元素在 2D 校验页有且仅有黄色高亮 + 问题清单入口；用户修正后生成 3D，墙体无缝、无穿模、门窗洞位置正确；
3. 输入一面墙校准为 3600mm 后，所有房间面积与标注尺寸误差 ≤ 1%；
4. 漫游模式下以最高速度正对墙体移动 10s 不发生穿墙；门可开合且开启状态下可通过；
5. 拖入沙发并贴左墙，与墙间距为 0；输入宽度改为 2400 后模型等比更新且不穿墙；
6. 给客厅墙面换色、地面换木纹材质，切换日/夜景各渲染 1 次，画面无明显错误；移动端操作全程帧率 ≥ 30fps；
7. 刷新页面后草稿完整恢复；分享链接在未登录手机浏览器可直接打开并流畅漫游。

---

## 12. 里程碑与排期建议（约 14 周）

| 阶段 | 周期 | 交付 |
|---|---|---|
| M0 原型验证 | 第 1~2 周 | three.js 手搭样板间（墙门窗 + 漫游 + 碰撞）；识别 pipeline 技术 spike，在 30 张图集上验证墙体 F1 能否达标 |
| M1 内部 Alpha | 第 3~6 周 | 上传→识别（墙/门/窗/尺寸）→2D 校准→3D 生成闭环；自动存草稿；80 件基础家具；纯色+基础材质 |
| M2 公测 Beta | 第 7~10 周 | 漫游打磨、家具拖拽/Gizmo、材质库、截图、分享链接、移动端适配、埋点；识别准确率达标调优 |
| M3 V1.0 发布 | 第 11~12 周 | 光照日照、智能布局初版、多方案、全景分享、性能与稳定性压测 |
| M4 商业化试点 | 第 13~14 周 | B 端水印/批量、家具商品信息对接、数据看板雏形 |

---

## 13. 主要风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 真实户型图质量参差（拍照反光、彩色填充、非标符号）导致识别不达标 | 高 | 强校验编辑兜底 + 半自动描墙；持续扩充测试集与回流样本；先宣称支持"线稿/置业图"，拍照为 beta |
| 尺寸标注缺失，比例尺不准 | 中 | 多线索估计（门宽、模数）+ 强制但低成本的一步校准，校准入口贯穿始终 |
| 复杂非正交/弧形墙、复式结构 | 中 | MVP 仅承诺正交单层户型；弧形/复式在 P2 并在校验页明确"暂不支持自动识别，可手动绘制" |
| 大户型移动端性能差 | 中 | 贴图/模型分级、默认简模、分辨率自适应、移动端关软阴影 |
| 家具模型版权与加载体积 | 中 | 采购/自建合规库，压缩管线；上传功能后置并加协议 |
| 3D 编辑对小白仍偏复杂 | 中 | 引导气泡、模板化一键方案、移动端极简模式、用数据迭代交互 |

---

## 14. 后续演进（Backlog 展望）

- **AI 文生家装**：自然语言生成整套软装与配色，多方案一键出图；
- **装修深化**：水电点位、开关插座、吊顶、铺砖排版，导出施工图；
- **预算与清单**：按家具/材料自动生成购物清单与报价，电商/供应链成交闭环；
- **BIM / CAD 互通**：导入 DWG、导出 IFC/DXF，面向设计师；
- **VR / AR**：VR 头显沉浸式看房；手机 AR 把家具 1:1 投到真实家中；
- **数字孪生**：接入真实房源数据、楼栋外景、小区环境，线上完整带看；
- **协作**：多人同时在 3D 房间里语音、批注、共同改方案。

---

## 附录 A：默认建筑参数表（可在设置中修改）

| 参数 | 默认值 |
|---|---|
| 层高 | 2800 mm |
| 外墙厚 / 内墙厚 | 200 / 100 mm |
| 入户门宽×高 | 1000 × 2100 mm |
| 室内门宽×高 | 800~900 × 2100 mm |
| 卫生间门宽 | 750 mm |
| 窗台高 / 窗高 | 900 / 1400 mm（落地窗窗台 0） |
| 阳台栏杆高 | 1100 mm |
| 人视高度 | 1600 mm（坐姿 1200） |
| 人物碰撞半径 | 250 mm |
| 默认比例尺兜底 | 入户门 900mm 反推 |
